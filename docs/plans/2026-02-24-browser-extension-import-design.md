# CPTracker 浏览器扩展设计文档

## 1. 项目概述

**项目名称**: CPTracker Web Clipper
**项目类型**: 浏览器扩展 (Chrome/Firefox)
**核心功能**: 从 Codeforces/AtCoder 题目页面一键导入题目到 CPTracker
**目标用户**: 竞技编程选手

## 2. 技术栈

- **框架**: WXT (Web Extension Framework)
- **前端**: React 19 + TypeScript
- **样式**: Tailwind CSS 4
- **构建**: Vite (via WXT)

## 3. 架构设计

### 3.1 整体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        Browser Extension                         │
├─────────────────────────────────────────────────────────────────┤
│  Content Script                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐       │
│  │ codeforces.js│  │  atcoder.js  │  │   parser.ts  │       │
│  │  (页面解析)   │  │  (页面解析)   │  │  (通用解析)   │       │
│  └──────┬───────┘  └──────┬───────┘  └──────────────┘       │
│         │                  │                                    │
│         └────────┬─────────┘                                    │
│                  ▼                                               │
│         ┌────────────────┐                                      │
│         │  content.ts    │ ────► 提取题目信息                    │
│         └────────┬───────┘                                      │
│                  │  message                                      │
├──────────────────┼──────────────────────────────────────────────┤
│                  ▼                                               │
│         ┌────────────────┐                                      │
│         │ background.ts  │ ────► clist.by API 调用               │
│         └────────┬───────┘                                      │
│                  │                                               │
├──────────────────┼──────────────────────────────────────────────┤
│                  ▼                                               │
│         ┌────────────────┐                                      │
│         │   Popup UI     │ ────► 用户确认/编辑 tags              │
│         │   (React)     │ ────► chrome.tabs.create 跳转        │
│         └────────────────┘                                      │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ chrome.storage.local (数据中转)
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                     CPTracker Web App                            │
├─────────────────────────────────────────────────────────────────┤
│  GET /import?source=extension                                 │
│  └─► 从 storage 读取数据，自动添加，跳转首页/关闭                │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 文件结构

```
extension/
├── entrypoints/
│   ├── popup/
│   │   ├── App.tsx          # 主应用组件
│   │   ├── main.tsx         # React 入口
│   │   ├── index.html       # HTML 模板
│   │   └── style.css        # Tailwind 入口
│   ├── content.ts           # Content Script 入口
│   └── background.ts        # Background Script 入口
├── utils/
│   ├── codeforces.ts        # Codeforces 页面解析
│   ├── atcoder.ts          # AtCoder 页面解析
│   ├── parser.ts            # 通用解析工具
│   ├── clist.ts            # clist.by API 调用
│   └── types.ts            # 类型定义
├── public/
│   └── icon/               # 扩展图标
├── wxt.config.ts           # WXT 配置
└── package.json
```

## 4. 功能设计

### 4.1 支持的 OJ 平台

| 平台 | 题目页面 URL 匹配 | 提交页面 URL 匹配 |
|------|-------------------|-------------------|
| Codeforces | `*://codeforces.com/contest/*/problem/*` | `*://codeforces.com/contest/*/submission/*` |
| AtCoder | `*://atcoder.jp/contests/*/tasks/*` | `*://atcoder.jp/contests/*/submissions/*` |

### 4.2 提取的信息

| 字段 | 来源 | 必填 |
|------|------|------|
| `url` | `window.location.href` | ✓ |
| `name` | DOM 提取 | ✓ |
| `rating` | clist.by API | 可选 |
| `tags` | DOM 提取 (仅 Codeforces) | 可选 |
| `solvedTime` | DOM 提取 (提交页面) | 可选 |
| `code` | DOM 提取 (提交页面) | 可选 |

### 4.3 Content Script 解析逻辑

#### Codeforces

**题目页面** (`/contest/{id}/problem/{index}`):
- 题目名称: `#pageContent > div.problemindexholder > div.ttypography > div > div.header > div.title`
- Tags: `#sidebar > div:nth-child(11) > div:nth-child(2) > div.roundbox > span`

**提交页面** (`/contest/{id}/submission/{id}`):
- 题目链接: `td:nth-child(3) > a`
- 解题代码: `#program-source-text`
- 通过时间: `td:nth-child(8)`

#### AtCoder

**题目页面** (`/contests/{id}/tasks/{slug}`):
- 题目名称: `#main-container > div.row > div:nth-child(2) > span.h2`

**提交页面** (`/contests/{id}/submissions/{id}`):
- 展开按钮: `p:nth-child(3) > a`
- 解题代码: `#submission-code > div.ace_scroller > div > div.ace_layer.ace_text-layer`
- 题目链接: `div:nth-child(8) > table > tbody > tr:nth-child(2) > td > a`
- 通过时间: `div:nth-child(8) > table > tbody > tr:nth-child(1) > td > time`

### 4.4 clist.by API 集成

- 用户在扩展设置中配置 API Key (存储在 `chrome.storage.local`)
- 获取难度方式:
  - Codeforces: `GET https://clist.by:443/api/v4/problem/?name={题目名}&resource__regex=codeforces`
  - AtCoder: `GET https://clist.by:443/api/v4/problem/?name={题目名}&resource__regex=atcoder.jp`
- 请求 header: `Authorization: [用户的 API Key]`

### 4.5 数据传递方式

**核心优化：使用 Storage 中转，避免 URL 长度限制**

1. 扩展端 (Popup) 点击保存时:
   - 将完整数据写入 `chrome.storage.local.set({ pendingImportData: payload })`
   - 使用 `chrome.tabs.create({ url: '/import?source=extension' })` 在新标签页打开

2. CPTracker 端:
   - 检测 `source=extension` 参数
   - 从 `chrome.storage.local` 读取 `pendingImportData`
   - 自动调用 `ProblemService.addProblem()` 添加题目
   - 显示成功反馈，3 秒后自动关闭或跳转到首页

## 5. 页面设计

### 5.1 Popup 界面

```
┌─────────────────────────────┐
│  CPTracker Import    ⚙️     │  <- Header + Settings
├─────────────────────────────┤
│  [Loading...] 或           │  <- 加载状态
│  ─────────────────────────  │
│  Platform: Codeforces       │  <- 自动检测
│  ─────────────────────────  │
│  Problem Name               │
│  [A. Game of Sums      ]   │  <- 可编辑，必填
│  ─────────────────────────  │
│  Difficulty          [🔑]   │
│  [1500                  ]   │  <- 自动获取 / 可编辑 / API Key 引导
│  ─────────────────────────  │
│  Tags                       │
│  [dp] [games] [+]       │  <- 可编辑
│  ─────────────────────────  │
│  ☐ Include Code             │  <- 解题代码
│  ─────────────────────────  │
│  [  Save to CPTracker  ]   │  <- 主按钮 (disabled 当 name 为空)
└─────────────────────────────┘
```

**UI 交互细节:**
- **Loading 状态**: 显示骨架屏，避免 "No problem detected" 闪烁
- **按钮禁用**: 当 name 为空时，Save 按钮置灰
- **API Key 引导**: 当 rating 为空且未配置 API Key 时，显示 🔑 图标引导用户设置

### 5.2 Settings 界面

```
┌─────────────────────────────┐
│  Settings            ←     │
├─────────────────────────────┤
│  clist.by API Key          │
│  [******************]      │
│  ─────────────────────────  │
│  CPTracker URL             │
│  [https://...]        (i)  │
│  ─────────────────────────  │
│  [   Save Settings    ]    │
└─────────────────────────────┘
```

### 5.3 CPTracker 导入成功页面

```
┌─────────────────────────────┐
│       ✓ 题目已添加!         │  <- 成功动画
│  ─────────────────────────  │
│  A. Game of Sums           │  <- 题目名称
│  Rating: 1500              │
│  ─────────────────────────  │
│  [ 查看题目 ]  [ 关闭页面 ]  │
│     (3秒后自动关闭)         │
└─────────────────────────────┘
```

## 6. CPTracker 后端改动

### 6.1 新增路由

**文件**: `src/routes/import.tsx`

**功能**:
1. 检测 `source=extension` 参数
2. 从 `chrome.storage.local` 读取 `pendingImportData`（通过 `window.postMessage` 或读取 URL 后再请求 storage）
3. 自动添加到数据库
4. 显示成功反馈，提供「关闭页面」选项

### 6.2 通信方式

由于跨域限制，CPTracker 无法直接读取扩展的 storage。解决方案：

**方案 A: 通过 URL 参数传递数据 ID**
```
/import?source=extension&id={uuid}
```
- 扩展写入 storage 时生成 uuid
- CPTracker 通过 id 查询 storage（需在扩展的 background script 中添加 API）

**方案 B: 直接读取 storage（推荐）**
- 在页面加载时通过 content script 注入数据
- 或者使用 `window.postMessage` 从页面传递

**最终方案:**
- 扩展使用 `chrome.tabs.create` 打开 `/import?source=extension`
- `/import` 页面加载后，通过 `window.postMessage` 向页面发送 `pendingImportData`
- 页面监听到消息后自动执行导入

## 7. 错误处理

| 场景 | 处理方式 |
|------|----------|
| 非 OJ 页面 | 显示提示「当前页面不支持」 |
| clist.by API 失败 | 显示 rating 为空，Input 旁显示 🔑 引导配置 API Key |
| CPTracker 添加失败 | 显示错误信息，提供重试按钮 |
| 必填字段缺失 | Save 按钮禁用 |
| Storage 无数据 | 提示「请重试导入」 |

## 8. 配置存储

使用 `chrome.storage.local` 存储:
- `clistApiKey`: clist.by API Key
- `cptrackerUrl`: CPTracker 部署地址 (默认: `https://competitive-programming-tracker.netlify.app`)
- `pendingImportData`: 待导入的题目数据 (JSON)

## 9. 待确定

- [ ] 是否需要登录认证？
- [ ] 扩展图标设计
